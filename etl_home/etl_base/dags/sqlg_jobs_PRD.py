
# -*- coding: utf-8 -*-
# Author        : Jesse Wei
# LastUpdate    : 2020/10/04
# Impact        : Jobs generated by SQLG
# Message       : Humanity towards others, we live by sharing. Fear can hold you prisoner, only hope can set you free.

# from __future__ import print_function
import logging
import airflow
from datetime import datetime, timedelta
from airflow.operators.sensors import ExternalTaskSensor
from airflow.operators.python_operator import PythonOperator
from airflow.operators.oracle_operator import OracleOperator

from airflow import models
from airflow.models import Variable

from acme.operators.dwh_operators import PostgresOperatorWithTemplatedParams


# DB_NAME = 'DWH'

my_taskid = "MTL_SYSTEM_ITEMS_B"
MTL_SYSTEM_ITEMS_B = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "FND_COLUMNS"
FND_COLUMNS = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "FND_LOOKUP_TYPES"
FND_LOOKUP_TYPES = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "FND_LOOKUP_VALUES"
FND_LOOKUP_VALUES = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "FND_TABLES"
FND_TABLES = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "MTL_CATEGORIES_B"
MTL_CATEGORIES_B = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "MTL_CATEGORY_SETS_B"
MTL_CATEGORY_SETS_B = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "MTL_CUSTOMER_ITEMS"
MTL_CUSTOMER_ITEMS = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "MTL_ITEM_CATALOG_GROUPS_B"
MTL_ITEM_CATALOG_GROUPS_B = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "MTL_ITEM_CATEGORIES"
MTL_ITEM_CATEGORIES = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "MTL_ITEM_STATUS_TL"
MTL_ITEM_STATUS_TL = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "MV_PROJECT_ACTIVITY"
MV_PROJECT_ACTIVITY = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "MV_XXPLM_CFDMETADATA"
MV_XXPLM_CFDMETADATA = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "MV_XXPLM_EC"
MV_XXPLM_EC = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "PRJ_WORKTIMEDATA"
PRJ_WORKTIMEDATA = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "XXPLM_MODEL"
XXPLM_MODEL = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "XXPLM_PROJECT"
XXPLM_PROJECT = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "XXPLM_TFD"
XXPLM_TFD = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "Z_CDOCUMENT_CHECKING_RULE"
Z_CDOCUMENT_CHECKING_RULE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "MV_XXPLM_MODEL_CHECKRULE_V"
MV_XXPLM_MODEL_CHECKRULE_V = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "XXPLM_EC_CHANGE_TYPE"
XXPLM_EC_CHANGE_TYPE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "NSP_REQ_HEADERS"
NSP_REQ_HEADERS = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "NSP_REQ_LINES"
NSP_REQ_LINES = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "PCS_HEADER"
PCS_HEADER = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "PCS_LINEEE"
PCS_LINEEE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "BTMS_EXPENSEPROJECT"
BTMS_EXPENSEPROJECT = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_EMPLOYEE_H"
SDM_EMPLOYEE_H = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_TEMPLATE_PRD"
SDM_TEMPLATE_PRD = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_PLM_CATEGORY"
SDM_PLM_CATEGORY = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_ITEM"
SDM_ITEM = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_ECN_REASON"
SDM_ECN_REASON = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_XXPLM_EC"
SDM_XXPLM_EC = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_ECN_CASE_AFTER_MP"
SDM_ECN_CASE_AFTER_MP = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_CDOC_COMPLETION_RATE"
SDM_CDOC_COMPLETION_RATE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_UPLOAD_CDOC_COUNT"
SDM_UPLOAD_CDOC_COUNT = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_TOTAL_CDOC_COUNT"
SDM_TOTAL_CDOC_COUNT = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_PROJECT_CODE"
SDM_PROJECT_CODE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_TOOLING_TOTAL_EXPENSE"
SDM_TOOLING_TOTAL_EXPENSE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_DMST_AND_INTL_TRAVEL_EXP"
SDM_DMST_AND_INTL_TRAVEL_EXP = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_TESTING_EXPENSE"
SDM_TESTING_EXPENSE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_CTF_EXPENSE"
SDM_CTF_EXPENSE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_EQT_EXPENSE"
SDM_EQT_EXPENSE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_EPR_MFG_SAMPLE_BUILD_EXP"
SDM_EPR_MFG_SAMPLE_BUILD_EXP = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_CDOC_PLANNED_DEV_TIME"
SDM_CDOC_PLANNED_DEV_TIME = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_PROD_DEV_MLST_DELAY_RATE"
SDM_PROD_DEV_MLST_DELAY_RATE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "SDM_CDOC_DELAY_TIME"
SDM_CDOC_DELAY_TIME = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "FCT_PROD_DEV_MLST_DELAY_RATE"
FCT_PROD_DEV_MLST_DELAY_RATE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "FCT_ECN_CASE_AFTER_MP"
FCT_ECN_CASE_AFTER_MP = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "FCT_TOOLING_TOTAL_EXPENSE"
FCT_TOOLING_TOTAL_EXPENSE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "FCT_TESTING_EXPENSE"
FCT_TESTING_EXPENSE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "DIM_ITEM"
DIM_ITEM = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "DIM_PLM_CATEGORY"
DIM_PLM_CATEGORY = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "DIM_ECN_REASON"
DIM_ECN_REASON = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


my_taskid = "DIM_PROJECT_CODE"
DIM_PROJECT_CODE = OracleOperator(
    task_id=my_taskid,
#    postgres_conn_id='oracle_default',
#    sql=DB_NAME + '/' + my_taskid + '/' + my_taskid + '.sql',
    sql=my_taskid + '/' + my_taskid + '.sql',
    parameters={"window_start_date": "{{ ds }}", "window_end_date": "{{ tomorrow_ds }}"},
#    start_date=airflow.utils.dates.days_ago(0),
    pool='default_pool')


